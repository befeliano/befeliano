#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
   float a,b,c;

   printf("Enter side A: ");
   scanf("%f", &a);

   printf("Enter side B: ");
   scanf("%f", &b);

   c = sqrt(a*a+b*b);

   printf("The hypotenuse is %f", c);
    return 0;
}
